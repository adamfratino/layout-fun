import styled from 'styled-components';
import { PROMO_HEIGHT, smallBlock } from '../../../StyleHelpers';

export const PromoContainer = styled.aside`
  ${smallBlock};
  background-color: rgb(244, 67, 54);
  height: ${PROMO_HEIGHT}px;
  justify-content: center;
`;
